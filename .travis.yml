language: ruby

services:
 - postgresql

rvm:
  - 2.6.2

cache: bundler

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - bundle install
  - bundle exec rake db:setup

script:
  - rspec
  - rubocop
